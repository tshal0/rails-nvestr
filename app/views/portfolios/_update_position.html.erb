  <div id="updatePosition" class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h4 id="modalHeader" class="center">Update Position</h4>
      </div>
      <!-- @all_attribs and @header_attribs -->
      <div class="modal-body">
        <%= form_for @position, url: update_position_path, remote: true do |f| %>
          <table align="center" class="table table-striped table-bordered">
            <tbody class="center">
              
            </tbody>
          </table>
          <%= f.submit "Update", :id => "updatePositionBtn", class: "btn btn-primary" %> 
        <% end %>
      </div>
    </div>
  </div>