$("#modal-window").html("<%= escape_javascript render(:partial => '/dangerous/login_modal') %>");
$('#modal-window').modal('show');

$(".button-submit").on('click', function() {
	// Hide the modal. 
	$("#modal-window").modal('hide');
	// The table refresh needs to happen AFTER the database has been updated. 

})

// $(".edit-personnel-btn").on("ajax:success", function (event) {
// 	console.log($(this).closest("tr").attr('id'))
// 	//$('#modal-window').attr('data-id', $(this).closest('tr').find('td').val())
// });

$('.modal ').insertAfter($('body'));