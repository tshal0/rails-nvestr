$("#modal-window").html("<%= escape_javascript render(:partial => '/users/manage_user') %>");
$('#modal-window').modal('show');

$("#manageUserModalBtn").on('click', function() {
	// Hide the modal. 
	$("#modal-window").modal('hide');
	// The table refresh needs to happen AFTER the database has been updated. 

})